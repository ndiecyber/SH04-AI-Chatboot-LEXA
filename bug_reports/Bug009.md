# Bug Report — Bug-009

**Project:** SH04-AI-Chatbot-LEXA  
**Bug ID:** Bug-009  
**Date Reported:** 2026-07-10  
**Reporter:** QA Engineering Team  
**Component:** `backend/services/chat_service.py`

---

## Title
Session Pipeline Cache (`_session_pipelines`) Tidak Memiliki Eviction — Potensi Memory Leak

## Severity
🟡 **Medium**

## Status History

| Date | Status | Actor | Notes |
|------|--------|-------|-------|
| 2026-07-10 | 🟡 Open | QA Team | Ditemukan saat audit v2.0 |
| 2026-07-10 | 🟡 Open | QA Team | Re-check kode — tidak ada TTL atau limit |

## Current Status: 🟡 **OPEN — Not Fixed**

---

## Description

`_session_pipelines` dict di `chat_service.py` menyimpan `RAGPipeline` per sesi tanpa batas waktu atau jumlah maksimum.

**Kode aktual:**
```python
_session_pipelines: Dict[str, RAGPipeline] = {}

def get_session_pipeline(session_id: str, base_pipeline: RAGPipeline) -> RAGPipeline:
    if session_id not in _session_pipelines:
        sess_pipeline = RAGPipeline()
        sess_pipeline.load_or_build()
        _session_pipelines[session_id] = sess_pipeline
    return _session_pipelines[session_id]
    # ← Tidak ada eviction, tidak ada TTL, tidak ada limit
```

**Re-test Status:** ❌ **FAIL — Bug Still Open**